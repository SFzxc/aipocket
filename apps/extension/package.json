{
  "name": "@aipocket/extension",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && cp public/manifest.json dist/manifest.json",
    "package": "npm run build && node scripts/package-extension.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aipocket/protocol": "0.0.0"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest"
  }
}
